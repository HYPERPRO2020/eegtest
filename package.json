{
  "name": "neuroqa",
  "private": true,
  "description": "Node dependency for the one JS function this app needs (api/blob-upload-token.js) -- everything else is Python. See ARCHITECTURE.md.",
  "dependencies": {
    "@vercel/blob": "^0.27.0"
  }
}
