# Deployed dependency set for every api/*.py function (they all share this
# directory's requirements.txt, per @vercel/python convention). Heavier than
# neuroqa/requirements.txt (webapp.py's single-file quick-grader doesn't need
# pandas/scikit-learn/statsmodels/autoreject) because these routes run the
# full upload -> validate -> score -> Study A -> Study B pipeline.
#
# See ARCHITECTURE.md section 1: measured with `pip install -t <dir>` (what
# Vercel's Python builder actually does), this full set is 398MB after
# stripping __pycache__, comfortably under the Python runtime's 500MB
# uncompressed function-size limit -- confirmed against Vercel's own current
# docs, not assumed. mne pulls in matplotlib as a hard runtime dependency now
# (not just a plotting extra, confirmed empirically) -- don't try to strip it.
Flask==3.1.3
Werkzeug==3.1.8
mne==1.12.1
numpy==2.4.6
scipy==1.17.1
scikit-learn==1.9.0
statsmodels==0.14.6
autoreject==0.4.4
pandas==3.0.5
requests==2.33.1
vercel_blob==0.4.2
