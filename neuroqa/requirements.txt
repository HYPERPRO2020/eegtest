# Deployed dependency set for webapp.py's single-file quick-grader route
# (analyze.py, manifest.py, preprocess.py, quality_index.py,
# artifact_detectors.py, bands.py, score.py, faa.py). Kept lean -- this
# route doesn't run Study A/B, so it skips pandas/scikit-learn/statsmodels/
# autoreject (see api/requirements.txt for the heavier set those need).
#
# See ARCHITECTURE.md section 1: mne pulls in matplotlib as a hard runtime
# dependency now (confirmed empirically, not just a declared "extra"), and
# the Python runtime's real limit is 500MB uncompressed (confirmed against
# Vercel's current docs), not the 250MB the previous version of this file
# assumed -- this set measures well under that either way.
Flask==3.1.3
Werkzeug==3.1.8
mne==1.12.1
numpy==2.4.6
scipy==1.17.1

# For the local batch CLI (run_local.py) and the api/*.py functions' own
# Study A/B pipeline -- not needed by webapp.py itself, so not required to
# run the single-file quick-grader. Install for local batch use:
#   pip install -r ../api/requirements.txt
