# Deployed dependency set for the whole app (webapp.py -- one Flask app,
# one Vercel Python function; see ARCHITECTURE.md's post-deploy notes for
# why it's one file/one function instead of splitting the quick-grader and
# the upload-batch job model apart: two live deploys hit the same Vercel
# build-infra bug with two separate @vercel/python builds, consistent with
# concurrent Python builds racing on Vercel's shared bytecode-cache
# directory -- one Python build removes the race by removing the
# concurrency).
#
# See ARCHITECTURE.md section 1: measured with `pip install -t <dir>` (what
# Vercel's Python builder actually does), this full set is 398MB after
# stripping __pycache__, comfortably under the Python runtime's 500MB
# uncompressed function-size limit -- confirmed against Vercel's own current
# docs, not assumed. mne pulls in matplotlib as a hard runtime dependency now
# (not just a plotting extra, confirmed empirically) -- don't try to strip it.
Flask==3.1.3
Werkzeug==3.1.8
mne==1.12.1
numpy==2.4.6
scipy==1.17.1
scikit-learn==1.9.0
statsmodels==0.14.6
autoreject==0.4.4
pandas==3.0.5
requests==2.33.1
vercel_blob==0.4.2
joblib==1.5.3

# This is also what run_local.py (the offline batch CLI) needs -- one
# requirements.txt for the whole project now, deployed or local.
